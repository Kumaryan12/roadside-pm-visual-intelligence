# Run artifacts

Future runs use `artifacts/runs/<run_id>/` for configs, split assignments,
leakage reports, metrics, predictions, models, figures, logs, and provenance.
Large artifacts should remain ignored or use dedicated artifact storage.

